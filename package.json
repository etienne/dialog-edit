{
  "name": "dialog-edit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export && mkdir out/api && cp api/index.php out/api/index.php",
    "test": "jest",
    "test:watch": "npm test -- --watch",
    "store:parentsToChildren": "node scripts/parentsToChildren.js"
  },
  "dependencies": {
    "next": "^10.0.3",
    "react": "^17.0.1",
    "react-autosize-textarea": "^7.1.0",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "babel-jest": "^26.6.3",
    "jest": "^26.6.3"
  }
}
